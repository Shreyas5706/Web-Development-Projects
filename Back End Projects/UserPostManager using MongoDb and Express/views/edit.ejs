<!Doctype html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="bg-zinc-900 text-white w-full min-h-screen p-10">
        <div class="w-full flex justify-end ">
            <a href="/logout" class="px-5 py-2 rounded-md bg-red-500 text-sm my-3  inline-block ">Log Out </a>
        </div>
        <h5 class="mb-5">Edit your post </h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea name="content" placeholder="What's on your mind ? "class="block p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" id=""><%= post.content %></textarea>
            <input class="px-5 py-2 rounded-md bg-yellow-500 text-sm mt-2 w-40 text-black " type="submit" value="Update post">
        </form>
    </div>
</body>
</html>