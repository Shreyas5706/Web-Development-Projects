<!Doctype html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="bg-zinc-900 text-white w-full min-h-screen p-10">
        <div class="w-full flex justify-end ">

            <a href="/logout" class="px-5 py-2 rounded-md bg-red-500 text-sm my-3  inline-block ">Log Out </a>
        </div>
        <h3 class="text-3xl ">Hello <%= user.name  %> </h3>
        <h5 class="mb-5">You Can now create a new post </h5>
        <form action="/post" method="post">
            <textarea name="content" placeholder="What's on your mind ? " class="block p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" id=""></textarea>
            <input class="px-5 py-2 rounded-md bg-blue-500 text-sm mt-2 w-40 " type="submit" value="Create post">
        </form>
        <div class="posts block mt-10">
            <h3 class="text-zinc-400 ">Your Posts</h3>
            <div class="postcontainer">
        <% user.post.reverse().forEach((element)=>{%>

                     <div class="post w-1/3 p-5 border-2 mb-4 border-zinc-600 mt-5 bg-zinc-800 ">
                        <h4 class="text-blue-500 mb-2 " >@<%= user.username  %></h4>
                        <p class="text-sm tracking-tight "><%= element.content %></p>
                        <small class="my-2 " ><%= element.likes.length %> likes</small>
                        <div class="btns flex gap-4 mt-5 ">
                            <a href="/like/<%= element._id %>" class="text-blue-500">
                                <%= element.likes.indexOf(user._id) ? "like " : "UnLike"  %>
                            </a>
                            <a href="/edit/<%= element._id %>" class="text-zinc-500">Edit </a>
                        </div>
                    </div> 
                <% }) %>
            </div> 
        </div>
    </div>

</body>

</html>
