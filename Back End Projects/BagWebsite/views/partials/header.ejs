<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display']">
    <nav class="w-full fixed flex justify-between items-center top-0 left-0 px-8 py-4 bg-white shadow-md z-10">
    <h3 class="text-2xl font-bold text-blue-500 tracking-wide">Scatch</h3>
    <% var loggedIn=typeof(loggedIn)!=='undefined' ?loggedIn:true%>
    <% if(loggedIn){%>
    <div class="flex gap-8 text-lg">
        <a href="/shop" class="hover:text-blue-400 transition-colors">Shop</a>
        <a href="/cart" class="hover:text-blue-400 transition-colors">Cart</a>
        <a href="#" class="hover:text-blue-400 transition-colors">My Account</a>
        <a class="text-red-600 hover:text-red-400 transition-colors" href="/users/Logout">Logout</a>
    </div>
    <%}%>
</nav>
